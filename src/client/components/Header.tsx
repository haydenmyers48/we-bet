import { Toolbar, Box } from '@mui/material';

function Header() {
    return (
        <>
        <Toolbar
            sx={{
                bgcolor: 'black'
            }}
        >

        </Toolbar>
        </>
    );
}

export default Header;