import { Toolbar, Box } from '@mui/material';

function Header() {
    return (
        <>
        <Toolbar
            sx={{
                bgcolor: 'black'
            }}
        >
            WeBet
        </Toolbar>
        </>
    );
}

export default Header;